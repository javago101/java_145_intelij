package lecture10;

public class PassingArgumentstoaMethod
{
    public static void main(String[] args)
    {
        //A parameter is a variable that we assign a value to when we call a method
        //Arguments is a values passed during function call
        /*
       | Aspect       | Parameters                                     | Arguments                                      |
|--------------|------------------------------------------------|------------------------------------------------|
| Definition   | Variables declared in a function's definition  | Actual values passed to a function when it's called |
| Purpose      | Define what inputs a function expects          | Provide concrete data for the function to use  |
| Location     | In the function declaration/definition          | In the function call/invocation                 |
| Timing       | Defined when the function is written            | Supplied when the function is executed          |
| Example in code | `def add(a, b):`                          | `result = add(5, 3)`                           |
| Scope        | Local to the function                           | Can be variables or literals from the calling scope |
| Number       | Fixed for a given function                      | Can vary (if default values or variable arguments are used) |
| Nature       | Placeholders or templates                       | Concrete values or references                    |
| Flexibility  | Can have default values                         | Must match or satisfy the parameter requirements |
| Naming       | Names are significant in the function body     | Names (if any) are only relevant in the calling context |
         */
    }
}
